<phone:PhoneApplicationPage 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone" 
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone" 
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:chartingToolkit="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit" 
    x:Class="WeightWatch.Views.MainPage"
    d:DesignWidth="480" d:DesignHeight="696" 
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    mc:Ignorable="d"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">
    
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/AppBar/appbar.add.rest.png" Text="add weight" Click="AppBarIconClick_AddWeight"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="Settings" Click="AppBarIconClick_Settings"/>
                <shell:ApplicationBarMenuItem Text="About" Click="AppBarIconClick_About"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid Background="Transparent">
        <!--Pivot Control-->
        <controls:Pivot Title="WEIGHT WATCH">
            <controls:PivotItem Header="progress">
                <Grid>
                    <Image Name="summary_arrowImage" Width="180" Height="260" Margin="32,34,244,0" VerticalAlignment="Top" />
                    <TextBlock Height="90" HorizontalAlignment="Left" Margin="228,92,0,0" Name="summary_weightTextBlock" Text="" VerticalAlignment="Top" Width="204" FontSize="80" TextAlignment="Center" />
                    <TextBlock Height="30" Margin="228,190,25,0" Name="summary_systemTextBlock" Text="" VerticalAlignment="Top" TextAlignment="Center" FontSize="22" Width="203" />
                    <TextBlock Height="170" HorizontalAlignment="Center" Margin="31,329,26,0" Name="summary_messageTextBlock" Text="" VerticalAlignment="Top" Width="399" TextWrapping="Wrap" />
                </Grid>
            </controls:PivotItem>

            <controls:PivotItem Header="graph">
                <chartingToolkit:Chart x:Name="weightChart" Style="{StaticResource WeightChart_Style}">
                    <chartingToolkit:AreaSeries
                        AnimationSequence="Simultaneous"
                        ItemsSource="{Binding WeightHistoryList}"
                        DependentValuePath="Weight"
                        IndependentValuePath="Date"
                        DataPointStyle="{StaticResource WeightChart_AreaDataPointStyle}"
                        PathStyle="{StaticResource WeightChart_AreaSeriesPathStyle}"/>
                    <chartingToolkit:Chart.Axes>
                        <chartingToolkit:DateTimeAxis 
                            Orientation="X" 
                            Margin="0,15,0,0"
                            AxisLabelStyle="{StaticResource WeightChart_DateTimeAxisStyle}" />
                        <chartingToolkit:LinearAxis
                            Orientation="Y" 
                            Margin="0,0,10,0"
                            ShowGridLines="True" />
                    </chartingToolkit:Chart.Axes>
                </chartingToolkit:Chart>
            </controls:PivotItem>

            <controls:PivotItem Header="details">
                <toolkit:LongListSelector
                    x:Name="weightLongListSelector"
                    ItemsSource="{Binding WeightHistoryGroup}"
                    IsFlatList="False">
                    <toolkit:LongListSelector.GroupHeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Border Background="{StaticResource PhoneAccentBrush}" Margin="8 10 0 13">
                                    <TextBlock Text="{Binding GroupHeader}" Style="{StaticResource PhoneTextGroupHeaderStyle}" VerticalAlignment="Bottom" HorizontalAlignment="Left" />
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </toolkit:LongListSelector.GroupHeaderTemplate>
                    <toolkit:LongListSelector.ItemTemplate>
                        <DataTemplate>
                            <Grid>                     
                                <toolkit:ContextMenuService.ContextMenu>
                                    <toolkit:ContextMenu>
                                        <toolkit:MenuItem Header="edit" Click="EditMenuItem_Click" />
                                        <toolkit:MenuItem Header="delete" Click="DeleteMenuItem_Click" />
                                    </toolkit:ContextMenu>
                                </toolkit:ContextMenuService.ContextMenu>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="15"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="175"/>
                                    <ColumnDefinition Width="25"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding DateStr}" Style="{StaticResource PhoneTextNormalStyle}" />
                                <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding WeightStr}" Style="{StaticResource PhoneTextNormalStyle}" />
                            </Grid>
                        </DataTemplate>
                    </toolkit:LongListSelector.ItemTemplate>
                </toolkit:LongListSelector>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>

</phone:PhoneApplicationPage>