<phone:PhoneApplicationPage 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone" 
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone" 
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:chartingToolkit="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit" 
    x:Class="WeightWatch.MainPage"
    d:DesignWidth="480" d:DesignHeight="696" 
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    mc:Ignorable="d"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/icons/appbar.add.rest.png" Text="add weight" Click="AppBarIconClick_AddWeight"/>
            <shell:ApplicationBarIconButton IconUri="/icons/appbar.feature.settings.rest.png" Text="settings" Click="AppBarIconClick_Settings"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <!--Pivot Control-->
        <controls:Pivot Title="WEIGHT WATCH">
            <controls:PivotItem Header="summary">
                <Grid></Grid>
            </controls:PivotItem>

            <controls:PivotItem Header="graph">
                <chartingToolkit:Chart x:Name="weightGraph" Style="{StaticResource ChartStyle}">
                    <chartingToolkit:Chart.Axes>
                        <chartingToolkit:DateTimeAxis 
                            x:Name="weightChart_dateTimeAxis" 
                            Orientation="X" 
                            Margin="0,15,0,0"
                            AxisLabelStyle="{StaticResource WeightChart_DateTimeAxisStyle}" />
                        <chartingToolkit:LinearAxis
                            x:Name="weightChart_linearAxis"
                            Orientation="Y" 
                            Margin="0,0,10,0"
                            ShowGridLines="True" />
                    </chartingToolkit:Chart.Axes>
                </chartingToolkit:Chart>
            </controls:PivotItem>

            <controls:PivotItem Header="details">
                <toolkit:LongListSelector
                    ItemsSource="{Binding WeightHistoryGroup}"
                    ItemTemplate="{StaticResource LongListSelector_Item}" 
                    GroupHeaderTemplate="{StaticResource LongListSelector_GroupHeader}"
                    IsFlatList="False">
                </toolkit:LongListSelector>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>

</phone:PhoneApplicationPage>